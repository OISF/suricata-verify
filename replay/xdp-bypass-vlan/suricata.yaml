%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - stats
        - flow

af-packet:
  - interface: default
    xdp-mode: soft
    bypass: yes

