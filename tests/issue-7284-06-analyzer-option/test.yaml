requires:
    pcap: false

args:
- --engine-analysis
- --simulate-ips
- --set detect.pass-applayer-flow-action=true

checks:
- filter:
    filename: rules.json
    count: 1
    match:
      id: 2
      type: "app_layer"
      flags[4]: "applayer"
