requires:
  min-version: 9

pcap: false

args:
  - --engine-analysis

exit-code: 1

checks:
  - filter:
      count: 1
      match:
        log_level: Error
        engine.message: "invalid flowbit command combination in the same signature: toggle and unset"
