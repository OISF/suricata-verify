pcap: ../eve-metadata/testmyids.pcap

requires:

  script:
    - grep METADATA_DEFAULTS src/output-json-alert.c > /dev/null

checks:

  - filter:
      count: 1
      match:
        event_type: alert
        not-has-key: [flow, http, alert.metadata, alert.rule]
