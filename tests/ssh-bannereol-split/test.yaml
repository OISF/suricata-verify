requires:
  features:
    - HAVE_LIBJANSSON

# disables checksum verification
args:
  - -k none

# TODO find right condition
checks:
  - filter:
      count: 1
      match:
        event_type: alert
        alert.signature_id: 1
