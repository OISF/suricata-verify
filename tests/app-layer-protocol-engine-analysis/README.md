Test the engine-analysis JSON output (rules.json) for the app-layer-protocol
keyword: single value, multi-value list with a mode qualifier, and a negated
list.

Expected result: each rule's app_layer_protocol object reports the resolved
protocols, mode and negation.
