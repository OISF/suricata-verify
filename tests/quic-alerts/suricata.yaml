%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - quic
        - alert

app-layer:
  protocols:
    quic:
      enabled: yes
