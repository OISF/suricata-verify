requires:
  min-version: 9

pcap: ../absent-04/empty.pcap

args:
  - --init-errors-fatal

exit-code: 1

checks:
  - shell:
      args: grep "must_error cannot be combined with other keywords" suricata.log | wc -l | xargs
      expect: 1
