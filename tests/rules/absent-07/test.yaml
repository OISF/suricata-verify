requires:
  min-version: 8

pcap: ../absent-04/empty.pcap

args:
  - --init-errors-fatal

exit-code: 1

checks:
  - shell:
      args: grep "or_else on a buffer with a transform that can fail" suricata.log | wc -l | xargs
      expect: 2
