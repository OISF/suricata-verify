Test `absent: error_or` with base64 transforms. Validates matching on
decode errors and on successful decode with matching content, and
verifies no alert when decode succeeds without a content match.
