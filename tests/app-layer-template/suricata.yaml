%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      types:
        - template
        - alert
        - flow

app-layer:
  protocols:
    template:
      enabled: yes
      detection-ports:
        dp: 7000
