%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - mqtt:
            string-log-limit: 1mb

app-layer:
  protocols:
    mqtt:
      enabled: yes
