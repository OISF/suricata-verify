requires:
  min-version: 8

pcap: ../dns-single-request-v1/input.pcap

checks:
  - filter:
      count: 1
      match:
        dns.type: request
  - filter:
      count: 1
      match:
        dns.type: answer
