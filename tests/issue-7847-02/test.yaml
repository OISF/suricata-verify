requires:
  min-version: 8

pcap: ../issue-7847-01/xor_packet_test.pcap

args:
  - -v
  - -k none
  - --set default-rule-path=${TEST_DIR}
  - --set security.lua.allow-rules=true

checks:
  - filter:
      count: 1
      match:
        event_type: alert
        alert.signature_id: 1
