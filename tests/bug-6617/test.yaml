pcap: ../filestore-v2.1-forced/suricata-update-pdf.pcap

requires:
  min-version: 7

args:
- -k none

checks:
- filter:
   count: 1
   match:
     event_type: fileinfo
- filter:
   count: 0
   match:
     fileinfo.stored: true
