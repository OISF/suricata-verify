requires:
  min-version: 8

pcap: ../enip-alert/enip_test1.pcap

# disables checksum verification
args:
- -k none --set stream.midstream=true

checks:
  - filter:
      count: 1
      match:
        event_type: flow
        app_proto: enip
