requires:
  min-version: 9

pcap: false

args:
  - --engine-analysis

checks:
  - filter:
      count: 1
      match:
        log_level: Warning
        engine.message: "inconsequential flowbit command combination in the same signature: unset and toggle"
