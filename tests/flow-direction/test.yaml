checks:
  - filter:
        count: 0
        match:
                event_type: fileinfo
                flow_direction: to_server
  - filter:
        count: 0
        match:
                event_type: http
                flow_direction: to_server
  - filter:
        count: 14
        match:
                event_type: flow
                not-has-key: flow_direction
