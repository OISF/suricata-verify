requires:
  min-version: 8

pcap: false

exit-code: 1

args:
  - --engine-analysis

checks:
  - filter:
      count: 1
      match:
        log_level: Error
        engine.message: "invalid flowbit command combination in the same signature"
