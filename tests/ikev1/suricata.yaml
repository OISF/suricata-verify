%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - ike
        - flow

app-layer:
  protocols:
    ike:
      enabled: yes
