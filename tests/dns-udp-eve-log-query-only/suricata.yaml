%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - dns:
            requests: yes
            responses: no
  - callback:
      nta:
        dns:
          enabled: yes
          requests: yes
          responses: no