requires:
  features:
    - HAVE_NSS

args:
- -k none

checks:
  - filter:
      count: 1
      match:
        event_type: fileinfo
        fileinfo.state: TRUNCATED
        fileinfo.stored: true
        app_proto: smb
