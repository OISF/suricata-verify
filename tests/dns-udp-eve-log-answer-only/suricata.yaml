%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - dns:
            requests: no
            responses: yes
  - callback:
      nta:
        dns:
          enabled: yes
          requests: no
          responses: yes