%YAML 1.1
---

app-layer:
  protocols:
    dns:
      enabled: yes
      tcp:
        enabled: yes
      udp:
        enabled: yes
outputs:
  - eve-log:
      enabled: yes
      types:
        - dns

