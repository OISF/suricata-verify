pcap: ../flowbit-oring/input.pcap

# need to be tested with Suricata 7 on latest MacOS
skip: true

requires:
  lt-version: 8

args:
 - -k none

checks:
  - filter:
      count: 1
      match:
        event_type: alert
  - filter:
      count: 1
      match:
        event_type: alert
        alert.signature_id: 1
