# SIP OPTIONS Method Test

This test verifies that SIP traffic using the OPTIONS method is correctly identified as SIP protocol.

## Test Purpose
The OPTIONS method is shared between SIP and HTTP protocols. This test ensures that when OPTIONS is used in a SIP context it is correctly identified as SIP protocol.

PCAP created with `create_pcap.py` script.
