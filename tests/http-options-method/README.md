# HTTP OPTIONS Method Test

This test verifies that HTTP traffic using the OPTIONS method is correctly identified as HTTP protocol.

## Test Purpose
The OPTIONS method is shared between SIP and HTTP protocols. This test ensures that when OPTIONS is used in an HTTP context, it is correctly identified as HTTP protocol.

PCAP created with `create_pcap.py` script.
