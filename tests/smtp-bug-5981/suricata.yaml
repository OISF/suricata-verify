%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      types:
        - files
        - smtp:
          extended: yes
          md5: [body, subject]
        - anomaly
  - file-store:
      version: 2
      enabled: yes
      force-filestore: yes
