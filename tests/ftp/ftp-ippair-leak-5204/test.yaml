requires:
  min-version: 8.0.7

args:
  - --runmode=single

checks:

- filter:
    count: 10
    match:
      event_type: flow
      app_proto: ftp
- filter:
    count: 20
    match:
      event_type: flow
      app_proto: ftp-data
