requires:
  min-version: 7

args:
  - -k none

checks:
- filter:
    count: 1
    match:
      event_type: tls
      tls.sni: vlscppe.microsoft.com
- filter:
    count: 1
    match:
      event_type: flow
      tcp.state: closed
