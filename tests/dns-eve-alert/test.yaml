requires:
  features:
    - HAVE_LIBJANSSON

checks:
  - filter:
      count: 1
      match:
        event_type: alert
        dns.query[0].type: query
  - filter:
      count: 1
      match:
        event_type: alert
        dns.answer.type: answer
