%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - mqtt:
            msg-log-limit: 10

app-layer:
  protocols:
    mqtt:
      enabled: yes
