%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      ethernet: yes
      types:
        - dns
        - flow
  - callback:
      ethernet: yes
      nta:
        dns:
          enabled:  yes