# Fragmented IMAP FETCH responses

Verify that FETCH literals are consumed incrementally instead of requiring the
entire literal in TCP reassembly. The capture covers a 6 MiB literal under a
4 MiB reassembly memcap, multiple fragmented literals in one response, and a
fragmented response-first flow.

The synthetic capture is generated by `writepcap.py`.
