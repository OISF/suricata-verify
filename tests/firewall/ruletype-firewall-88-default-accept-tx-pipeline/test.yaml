requires:
  min-version: 8

pcap: input.pcap

args:
  - --simulate-ips
  - -k none

checks:
  - filter:
      count: 1
      match:
        event_type: alert
        alert.signature_id: 5002
        alert.action: blocked
        http.url: /foo/
