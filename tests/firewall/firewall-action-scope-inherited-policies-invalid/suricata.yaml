%YAML 1.1
---

engine-analysis:
  rules-fast-pattern: no
  rules: yes

logging:
  outputs:
    - file:
        enabled: yes
        filename: suricata.log
        
firewall:
  policies:
    default-policy: [ "accept:hook", "pass" ]
