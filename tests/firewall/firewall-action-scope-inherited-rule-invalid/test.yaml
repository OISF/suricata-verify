requires:
  min-version: 9
  pcap: false

args:
  - --engine-analysis

exit-code: 1

checks:
  - shell:
      args: grep -c "invalid action scope 'hook' in action 'pass'" suricata.log | xargs
      expect: 1
