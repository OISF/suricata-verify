requires:
  min-version: 9
  pcap: false

args:
  - --engine-analysis

exit-code: 1

checks:
  - shell:
      args: "grep -c \"'pass' is only supported as a secondary action for 'accept'\" suricata.log | xargs"
      expect: 1
