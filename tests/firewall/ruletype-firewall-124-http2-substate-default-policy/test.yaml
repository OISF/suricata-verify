requires:
  min-version: 9

pcap: ../../http2-userinfo-authority/http2_userinfo_in_authority_1.pcap

args:
  - --simulate-ips
  - -k none

checks:
- filter:
    count: 0
    match:
      event_type: drop
