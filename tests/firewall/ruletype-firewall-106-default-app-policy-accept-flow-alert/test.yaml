requires:
  min-version: 8.0.6

pcap: ../../flowbit-oring/input.pcap

args:
  - --simulate-ips
  - -k none

checks:

# We should have one http request.
- filter:
    count: 1
    match:
      event_type: http
