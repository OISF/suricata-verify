# Packet rules

# allow session setup
pass:packet tcp:all $HOME_NET any <> $EXTERNAL_NET 443 (flow:not_established; alert; sid:1021;)

# pass rest of the flow. Bidir as we don't know which side will talk first.
pass:flow tcp:all $HOME_NET any <> $EXTERNAL_NET 443 (flow:established; alert; sid:1023;)

# default drop
