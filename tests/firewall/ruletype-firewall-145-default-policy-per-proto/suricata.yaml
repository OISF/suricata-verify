%YAML 1.1
---

stats:
  enabled: yes
  interval: 8

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - stats
        - flow
        - alert
        - dns
        - drop:
            alerts: yes
            flows: all

firewall:
  policies:
    packet:
      default-policy: [ "accept:hook" ]
    app:
      dns:
        default-policy: [ "drop:flow" ]
        request-started: [ "accept:hook" ]
