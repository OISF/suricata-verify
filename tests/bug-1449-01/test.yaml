requires:
  features:
    - HAVE_LIBJANSSON

checks:
  - filter:
      count: 1
      match:
        event_type: alert
        alert.signature_id: 2220017
  - filter:
      count: 1
      match:
        event_type: smtp
        smtp.helo: bug.client
        email.status: PARSE_ERROR

