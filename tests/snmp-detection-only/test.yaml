args:
  - -k none

pcap: ../snmp-v2c-get/SNMPv2c_get_requests.pcap

requires:
  min-version: 9.0.0

checks:
 - filter:
     count: 4
     match:
       event_type: flow
       app_proto: snmp
 - filter:
     count: 0
     match:
       event_type: alert
       app_proto: snmp
