requires:
  features:
    - HAVE_LIBJANSSON
  min-version: 5.0.0

# disables checksum verification
args:
  - -k none

checks:
  - filter:
      count: 1
      match:
        event_type: flow
        app_proto: http
