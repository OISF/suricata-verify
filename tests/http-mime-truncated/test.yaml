requires:
  min-version: 7.0.0

# disables checksum verification
args:
  - -k none

checks:

  - filter:
      count: 1
      match:
        app_proto: http
        fileinfo.state: TRUNCATED
        fileinfo.size: 101422
