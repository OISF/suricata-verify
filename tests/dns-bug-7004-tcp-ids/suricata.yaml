%YAML 1.1
---

detect:
  guess-applayer-tx: yes

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      buffer-size: 0
      types:
        - alert
        - dns
