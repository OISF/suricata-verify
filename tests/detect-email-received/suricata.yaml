%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filename: eve.json
      types:
        - smtp:
            custom: [received]    # for 'received' logging information
        - alert:
            smtp: yes     # enable dumping of smtp fields
