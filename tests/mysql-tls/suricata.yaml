%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - mysql
        - tls

app-layer:
  protocols:
    mysql:
      enabled: yes
    tls:
      enabled: yes
