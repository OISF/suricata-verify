%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - rfb
        - flow

app-layer:
  protocols:
    rfb:
      enabled: yes
      detection-ports:
        dp: 5900
