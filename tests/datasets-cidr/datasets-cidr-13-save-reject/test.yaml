requires:
  min-version: 9
  pcap: false

exit-code: 1

args:
  - --engine-analysis

checks:
  - shell:
      args: grep "save/state is not supported for CIDR datasets" suricata.log | wc -l | xargs
      expect: 1
