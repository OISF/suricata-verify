requires:
  lt-version: 8
  features:
    - HAVE_LUA

args:
  - -v
  - -k none
  - --set default-rule-path=${TEST_DIR}
  - --set security.lua.allow-rules=true

checks:
  - filter:
      count: 1
      match:
        event_type: alert
        alert.signature_id: 1
