pcap: ../../dns-single-request-v1/input.pcap

requires:
  lt-version: 8

checks:
  - filter:
      count: 1
      match:
        dns.type: query
  - filter:
      count: 1
      match:
        dns.type: answer
