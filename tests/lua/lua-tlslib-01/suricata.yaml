%YAML 1.1
---

outputs:
  - lua:
      enabled: yes
      scripts-dir: .
      scripts:
         - lua-tlsfunctions.lua
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - alert
        - http
        - flow
        - tls
