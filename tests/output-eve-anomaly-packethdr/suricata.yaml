%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      types:
        - anomaly:
            types:
              packet: yes
            packethdr: yes            # enable dumping of packet header
