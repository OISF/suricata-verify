%YAML 1.1
---

requires:
  features:
    - HAVE_NSS

pcap-file:
    checksum-checks: no

outputs:
  - file-store:
      enabled: yes       # set to yes to enable
      version: 2
      force-filestore: yes # force storing of all files
