requires:
  min-version: 8

checks:
  - filter:
      count: 2
      match:
        event_type: dns
        dns.type: request
  - filter:
      count: 3
      match:
        event_type: dns
        dns.type: answer
        dns.answers.__len: 12
