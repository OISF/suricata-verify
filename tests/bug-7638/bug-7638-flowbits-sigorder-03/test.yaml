requires:
    min-version: 9

pcap: false

args:
  - --engine-analysis

exit-code: 1

checks:
  - filter:
      count: 1
      match:
        log_level: Error
        engine.message.__find: "Cyclic dependency found between flowbits from signatures"
