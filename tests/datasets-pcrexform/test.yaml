requires:
  features:
    - HAVE_LIBJANSSON
  files:
    - src/datasets.c

args:
 - -k none --data-dir="${OUTPUT_DIR}"

checks:
  - file-compare:
      filename: uri-param-seen.csv
      expected: expected/uri-param-seen.csv
