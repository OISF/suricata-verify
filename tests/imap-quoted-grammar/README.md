# IMAP quoted-string grammar

This test verifies that escaped quotes and backslashes are accepted in IMAP
quoted strings, and that parentheses inside quoted strings do not end request
lists or FETCH responses.
