# SWF decompression depth wrap regression

Purpose: exercise HTTP `file_data` inspection of a compressed SWF response while
SWF decompression is enabled with the maximum supported `decompress-depth` value
of 2147483640 bytes. This leaves room for the 8-byte FWS header while keeping the
header-inclusive inspection-buffer size at or below 2 GiB.

The pcap is generated by `make-pcap.py` with Scapy. It contains a single HTTP
transaction returning a small zlib-compressed `CWS` body. The rule uses
`file_data`, which causes the HTTP SWF decompression path to be reached.

SHA256: `1aee2558888d337904face20909e318298440604d19b9fbcc00e1f16d8d7e626`.

Older code did not enforce a safe upper bound and could wrap the internal FWS
buffer-length calculation. The regression verifies that the largest accepted
depth can process the response without overflowing the inspection buffer.

PCAP generation script created by GPT 5.5 with Codex.

Ticket: https://redmine.openinfosecfoundation.org/issues/8642
