%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - pgsql:
            enabled: yes
            #passwords: no   # enable output of passwords Default is false
        - flow

app-layer:
  protocols:
    pgsql:
      enabled: yes
