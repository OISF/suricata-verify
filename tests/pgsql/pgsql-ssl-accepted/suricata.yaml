%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - tls
        - pgsql
        - flow

app-layer:
  protocols:
    tls:
      enabled: yes
    pgsql:
      enabled: yes
