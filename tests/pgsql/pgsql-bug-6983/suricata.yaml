%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - pgsql
        - alert

app-layer:
  protocols:
    pgsql:
      enabled: yes
