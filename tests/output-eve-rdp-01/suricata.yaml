%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - rdp
      community-id: true

app-layer:
  protocols:
    rdp:
      enabled: yes
