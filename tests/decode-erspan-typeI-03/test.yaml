requires:

  min-version: 5.0.0


args:
    - --set decoder.erspan.typeI.enabled=false

checks:

    - filter:
        count: 0
        match:
            event_type: flow

    - stats:
        decoder.erspan: 0
