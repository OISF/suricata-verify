%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - anomaly:
            enabled: yes
        - bittorrent-dht
        - flow

app-layer:
  protocols:
    bittorrent-dht:
      enabled: yes
