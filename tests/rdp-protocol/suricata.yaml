%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - alert
        - rdp
        - flow
  - callback:
      nta:
        rdp:
          enabled: yes
app-layer:
  protocols:
    rdp:
      enabled: yes
