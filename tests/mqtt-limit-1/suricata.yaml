%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - mqtt

app-layer:
  protocols:
    mqtt:
      enabled: yes
      max-msg-length: 50000