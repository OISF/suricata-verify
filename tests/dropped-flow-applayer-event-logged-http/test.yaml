pcap: ../exception-policy-midstream-06/input-http-ACK.pcap

args:
- --simulate-ips
- --set stream.midstream=true
- -k none

checks:
  - filter:
      requires:
        min-version: 8.0.4
      count: 0
      match:
        event_type: http
        pcap_cnt: 2
  - filter:
      requires:
        min-version: 8.0.4
      count: 2
      match:
        event_type: drop
  - filter:
      requires:
        lt-version: 8.0
      count: 1
      match:
        event_type: http
        pcap_cnt: 2
  - filter:
      requires:
        lt-version: 8.0
      count: 1
      match:
        event_type: drop
  - filter:
      count: 1
      match:
        event_type: flow
        flow.action: drop

