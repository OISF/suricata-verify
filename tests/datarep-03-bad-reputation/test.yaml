requires:
  # No pcap required.
  pcap: false

exit-code: 1

args:
  - --engine-analysis

checks:
  - shell:
      requires:
        min-version: 8
      args: grep "invalid datarep value" suricata.log | wc -l | xargs
      expect: 1
