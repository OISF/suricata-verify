requires:
  min-version: 8.0.7

args:
- --engine-analysis

exit-code: 0

pcap: false

checks:
  - filter:
      filename: rules.json
      count: 1
      match:
        id: 1000001
        type: like_ip_only
