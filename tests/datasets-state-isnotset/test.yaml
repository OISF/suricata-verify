requires:
  min-version: 5.0.0
  features:
    - HAVE_NSS
    - HAVE_LIBJANSSON

pcap: false

args:
  - --data-dir=${OUTPUT_DIR}

checks:
  - filter:
      count: 1
      match:
        alert.signature_id: 1 
