pcap: ../filestore-filecontainer-http/filecontainer-http.pcap

requires:
  features:
    - MAGIC

checks:
    - filter:
        count: 1
        match:
            event_type: alert
            alert.signature_id: 1