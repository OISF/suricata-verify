%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      types:
        - alert
        - tls
  - tls-store:
      enabled: yes
      certs-log-dir: certs # directory to store the certificates files

