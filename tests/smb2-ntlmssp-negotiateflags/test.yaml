requires:
  features:
    - HAVE_LIBJANSSON
    - RUST

# disables checksum verification
args:
- -k none

checks:
  - filter:
      count: 7
      match:
        event_type: smb
        smb.ntlmssp.version: "10.0 build 10586 rev 15"
