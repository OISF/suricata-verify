pcap: ../test.pcap

args:
- --set flow.memcap-policy=drop-flow
- --set flow.prealloc=0
- --set flow.memcap=3kb
- --set flow.hash-size=32
- --set flow.force-reuse=true

checks:
  - shell:
      args: grep -c 'flow[.]get_used ' stats.log
      expect: 1
