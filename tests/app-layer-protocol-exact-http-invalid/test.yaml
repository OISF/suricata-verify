requires:
  min-version: 8.0.7
  pcap: false

args:
  - --engine-analysis

exit-code: 1

checks:
  - shell:
      args: grep -q "'http' with 'exact' never" suricata.log && echo found
      expect: found
