%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - mqtt
        - anomaly

app-layer:
  protocols:
    mqtt:
      enabled: yes

stream:
  midstream: true
