%YAML 1.1
---

outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - alert
        - flow

vlan:
  use-for-tracking: no

af-packet:
  - interface: default
    xdp-mode: soft
    bypass: yes
